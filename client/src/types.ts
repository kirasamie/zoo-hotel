export type EXAMPLETYPE = {
  someValue: number;
};

export type RoomType = {
  id: number;
  roomAbout: string;
  roomPhoto: string;
  roomCapacity: number;
  roomPetType: number;
  roomPrice: number;
};

export type RoomsType = RoomType[];